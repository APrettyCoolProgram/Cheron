<Window
    x:Class="Ubiquitus.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Ubiquitus"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Ubiquitus - Text Adventure Engine"
    Width="900"
    Height="600"
    Background="#1E1E1E"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,10"
            FontFamily="Consolas"
            FontSize="20"
            FontWeight="Bold"
            Foreground="#00D4AA"
            Text="UBIQUITUS TEXT ADVENTURE" />

        <Border
            Grid.Row="1"
            Margin="0,0,0,10"
            Background="#252526"
            BorderBrush="#3C3C3C"
            BorderThickness="1"
            CornerRadius="5">
            <ScrollViewer
                x:Name="OutputScrollViewer"
                VerticalScrollBarVisibility="Auto">
                <TextBlock
                    x:Name="OutputTextBlock"
                    Padding="15"
                    FontFamily="Consolas"
                    FontSize="14"
                    Foreground="#D4D4D4"
                    LineHeight="22"
                    Text="Welcome to Ubiquitus! Type 'help' for available commands."
                    TextWrapping="Wrap" />
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Margin="0,0,10,0"
                VerticalAlignment="Center"
                FontFamily="Consolas"
                FontSize="16"
                FontWeight="Bold"
                Foreground="#00D4AA"
                Text="&gt;" />

            <TextBox
                x:Name="CommandInput"
                Grid.Column="1"
                Padding="10,8"
                Background="#252526"
                BorderBrush="#3C3C3C"
                BorderThickness="1"
                CaretBrush="#D4D4D4"
                FontFamily="Consolas"
                FontSize="14"
                Foreground="#D4D4D4"
                KeyDown="CommandInput_KeyDown" />

            <Button
                x:Name="SendButton"
                Grid.Column="2"
                Margin="10,0,0,0"
                Padding="20,8"
                Background="#00D4AA"
                BorderThickness="0"
                Click="SendButton_Click"
                Content="Send"
                FontFamily="Segoe UI"
                FontSize="14"
                FontWeight="SemiBold"
                Foreground="#1E1E1E">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#00D4AA" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="5"
                                        Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#00BAAA" />
                                <Setter Property="Cursor" Value="Hand" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</Window>
