## application-data/dungine/config/

The `application-data/dungine/config/` folder contains any Dungine configuration data.

The `application-data/dungine/config/` folder may contain subfolders for specific configuration data.