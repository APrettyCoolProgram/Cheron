## application-data/dungine/cache/

The `application-data/dungine/cache/` folder contains any Dungine cached data.

Cached data is persistant. Temporary data should be stored in `application-data/dungine/tmp/`

The `application-data/dungine/cache/` folder may contain subfolders for specific cached data.