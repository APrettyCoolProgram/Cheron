## application-data/dungine/tmp/

The `application-data/dungine/tmp/` folder contains any Dungine temporary data.

Temporary data is not persistant. Persistand data should be stored in `application-data/dungine/cache/`

The `application-data/dungine/tmp/` folder may contain subfolders for specific temporary data.