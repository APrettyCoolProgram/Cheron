## application-data/dungine/runtime/

The `application-data/dungine/runtime/` folder contains any data that Dungine requires at runtime.

The `application-data/dungine/runtime/` folder may contain subfolders for specific runtime data.